<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leetcode.leetcode_ai.user.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.leetcode.leetcode_ai.user.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="nickname" column="nickname"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUsername" resultMap="userResultMap">
        select id, username, password_hash, nickname, avatar_url, created_at, updated_at
        from users
        where username = #{username}
        limit 1
    </select>

    <insert id="insert" parameterType="com.leetcode.leetcode_ai.user.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        insert into users (username, password_hash, nickname, avatar_url, created_at, updated_at)
        values (#{username}, #{passwordHash}, #{nickname}, #{avatarUrl}, now(), now())
    </insert>

</mapper>
